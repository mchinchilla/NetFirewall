@{
    ViewData["Title"] = "DHCP — Pool / classes";
    Layout = "_Layout";
    var pool = (NetFirewall.Models.Dhcp.DhcpPool)ViewBag.Pool;
    var subnetName = (string)ViewBag.SubnetName;
    var subnetId = (Guid)ViewBag.SubnetId;
}

<div class="flex flex-wrap items-end justify-between gap-3 mb-4"
     style="animation: var(--animate-slide-up);">
    <div>
        <p class="text-xs uppercase tracking-wider font-semibold text-surface-fg-muted">
            <a href="@Url.Action("Index", "DhcpSubnets")" class="hover:underline text-accent">DHCP · Subnets</a>
            <span class="text-surface-fg-muted"> / </span>
            <a href="/Dhcp/Subnets/@subnetId/Pools" class="hover:underline text-accent">@subnetName</a>
        </p>
        <h1 class="text-2xl font-semibold tracking-tight">Pool ↔ Classes</h1>
        <p class="text-sm mt-1 max-w-2xl text-surface-fg-muted">
            Pool <span class="font-mono">@pool.RangeStart – @pool.RangeEnd</span>. Toggle each class to allow or deny — unbound classes follow the global match rules.
        </p>
    </div>
</div>

<div class="flex flex-wrap items-center gap-2 mb-4"
     style="animation: var(--animate-slide-up); animation-delay: 40ms;">
    @await Html.PartialAsync("_TableSearch", new TableSearchModel { TableId = "pool-classes-table", Placeholder = "Filter by class, match…", ClientSide = true })
</div>

<section class="card" style="animation: var(--animate-slide-up); animation-delay: 80ms;">
    <div id="pool-classes-table"
         hx-get="/Dhcp/Subnets/@subnetId/Pools/@pool.Id/Classes/table"
         hx-trigger="load, refreshPoolClasses from:body"
         hx-swap="innerHTML">
        @await Html.PartialAsync("_LoadingOverlay", new LoadingOverlayModel { Variant = LoadingOverlayVariant.Placeholder })
    </div>
</section>
